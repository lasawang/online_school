# 前端设计文档

基于 Figma 设计: [Quiz Game (Community)](https://www.figma.com/design/zBUccvNJHt9MVMTwCmCqXJ/Quiz-Game--Community-)

## 🎨 设计系统

### 颜色方案

```css
--primary-color: #1935CA;        /* 主色调 */
--secondary-color: #5872FA;      /* 次要色 */
--background-color: #FBF9F9;     /* 背景色 */
--card-background: #FFFFFF;      /* 卡片背景 */
--text-primary: #181B1A;         /* 主要文字 */
--text-secondary: #696F79;       /* 次要文字 */
--text-tertiary: #B3B6B6;        /* 第三级文字 */
--border-color: #F5F5F5;         /* 边框色 */
--success-color: #6FD181;        /* 成功色 */
--warning-color: #FF7262;        /* 警告色 */
--info-color: #1ABCFE;           /* 信息色 */
```

### 字体

- **主字体**: Poppins
- **字重**: 
  - Light: 300
  - Regular: 400
  - Medium: 500
  - Semi-Bold: 600
  - Bold: 700
  - Black: 900

### 圆角

- 小圆角: 8px
- 中圆角: 12px
- 大圆角: 16px
- 圆形按钮: 50px

### 阴影

```css
box-shadow: 0px 5px 14px 2px rgba(237, 237, 237, 1);
```

## 📐 布局结构

### 主布局

- **侧边栏宽度**: 250px
- **顶部导航高度**: 70px
- **内容区域**: 自适应

### 响应式断点

- **Mobile**: < 768px
- **Tablet**: 768px - 992px
- **Desktop**: > 992px
- **Large Desktop**: > 1200px

## 🧩 组件设计

### 1. Header (顶部导航)

**组件**: `components/Layout/Header.tsx`

**特性**:
- Logo + 搜索框 + 通知 + 用户信息
- 固定在顶部
- 白色背景，带阴影

### 2. Sidebar (侧边栏)

**组件**: `components/Layout/Sidebar.tsx`

**特性**:
- 导航菜单
- 支持卡片
- 退出登录按钮
- 固定在左侧

### 3. CourseCard (课程卡片)

**组件**: `components/CourseCard/index.tsx`

**特性**:
- 封面图片
- 课程标题
- 时长标签
- 学员数量
- 评分
- 标签
- 悬停效果（提升 + 阴影）

### 4. StatsCard (统计卡片)

**特性**:
- 图标
- 数值
- 标题
- 渐变背景（可选）

## 📱 页面设计

### 1. Dashboard (首页)

**路径**: `/`

**布局**:
```
┌─────────────────────────────────┐
│  欢迎区域 (渐变背景)              │
├─────────────────────────────────┤
│  统计卡片 (3列)                  │
├─────────────────────────────────┤
│ ┌─────────────┐  ┌───────────┐  │
│ │  精选课程    │  │  提醒事项  │  │
│ │  (网格)      │  │           │  │
│ │             │  │  成就徽章  │  │
│ │  在线用户    │  │           │  │
│ └─────────────┘  └───────────┘  │
└─────────────────────────────────┘
```

### 2. CourseList (课程列表)

**路径**: `/courses`

**布局**:
```
┌─────────────────────────────────┐
│  Hero Section (蓝色渐变 + 搜索)   │
├─────────────────────────────────┤
│  热门标签                         │
├─────────────────────────────────┤
│  分类标签页                       │
├─────────────────────────────────┤
│  课程网格 (响应式 2-4列)          │
├─────────────────────────────────┤
│  加载更多按钮                     │
└─────────────────────────────────┘
```

### 3. CourseDetail (课程详情)

**路径**: `/courses/:id`

**布局**:
```
┌─────────────────────────────────┐
│  视频播放器                       │
├─────────────────────────────────┤
│ ┌─────────────┐  ┌───────────┐  │
│ │  课程信息    │  │  章节列表  │  │
│ │  描述       │  │           │  │
│ │  评论区      │  │           │  │
│ └─────────────┘  └───────────┘  │
└─────────────────────────────────┘
```

### 4. Login/Register (登录/注册)

**路径**: `/login`, `/register`

**特性**:
- 左侧：装饰图片/插画
- 右侧：表单
- 渐变背景
- Logo在顶部

## 🎯 交互效果

### 悬停效果

**卡片**:
```css
hover {
  transform: translateY(-5px);
  box-shadow: 0px 8px 20px 4px rgba(237, 237, 237, 1);
}
```

**按钮**:
```css
hover {
  background-color: #5872FA;
}
```

### 过渡动画

所有交互元素使用 `transition: 0.3s` 平滑过渡

## 📦 已实现组件

- ✅ Header (顶部导航)
- ✅ Sidebar (侧边栏)
- ✅ CourseCard (课程卡片)
- ✅ Home (首页/Dashboard)
- ✅ CourseList (课程列表)
- ⏳ CourseDetail (课程详情) - 待实现
- ⏳ VideoPlayer (视频播放器) - 待实现
- ⏳ LivePlayer (直播播放器) - 待实现

## 🚀 使用指南

### 1. 启动项目

```bash
cd frontend
npm install
npm run dev
```

### 2. 访问

```
http://localhost:3000
```

### 3. 页面路由

- 首页: `/`
- 课程列表: `/courses`
- 课程详情: `/courses/1`
- 登录: `/login`
- 注册: `/register`

## 📝 开发规范

### CSS类命名

使用 BEM 命名规范：
```css
.block {}
.block__element {}
.block--modifier {}
```

### 组件文件结构

```
ComponentName/
  ├── index.tsx      # 组件逻辑
  ├── index.css      # 组件样式
  └── types.ts       # 类型定义（可选）
```

### 颜色使用

优先使用 CSS 变量：
```css
color: var(--primary-color);
```

## 🎨 Figma 设计参考

设计中提取的关键元素：
- 蓝色主色调 (#1935CA)
- Poppins 字体
- 卡片式布局
- 圆角设计 (8-16px)
- 柔和阴影效果
- 清晰的视觉层级

## 📌 注意事项

1. 所有文字使用 Poppins 字体
2. 保持统一的间距和圆角
3. 使用设计系统中定义的颜色
4. 响应式设计优先
5. 确保良好的用户体验


